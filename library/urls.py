from django.conf.urls import url
# from .views import AuthorListView, AuthorDetailView, AuthorDetailView

app_name = 'main_app'

urlpatterns = [
    # url(r'^$', AuthorListView.as_view(), name='author'),
    # url(r'^(?P<pk>[0-9]+)/$', AuthorDetailView.as_view(), name='detail'),
    # url(r'^create/$', AuthorCreate.as_view(), name='author_create'),
    # url(r'(?P<pk>[0-9]+)/update/$', AuthorUpdate.as_view(), name='author-update'),
    # url(r'(?P<pk>[0-9]+)/delete/$', AuthorDelete.as_view(), name='author-delete'),
]